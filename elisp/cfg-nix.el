;;; cfg-nix.el --- configfuration for nix and nixos -*- lexical-binding: t -*-
;;; Commentary:

;; Everything what is connected with nix and NixOS
;;; Code:

(use-package nix-mode
  :ensure t
  )

(provide 'cfg-nix)
;;; cfg-nix.el ends here
